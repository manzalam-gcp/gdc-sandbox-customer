apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  namespace: tack-1
  name: allow-external-traffic-ingress
spec:
  podSelector:
    matchLabels:
      app: elasticsearch
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: kibana
