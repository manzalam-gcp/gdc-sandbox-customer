apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  namespace: tack-1
  name: allow-external-traffic-ingress
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  ingress:
  - from:
    - ipBlock:
        cidr: 0.0.0.0/0
