apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  namespace: ${WORKLOAD_PROJECT}
  name: allow-workload-open
spec:
  podSelector:
    matchLabels:
      app: ollama
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: open-webui